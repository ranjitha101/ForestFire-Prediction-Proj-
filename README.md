# ForestFire-Prediction-Proj-


This AI project predicts forest fires using environmental factors like temperature and humidity. It uses machine learning models such as logistic regression to classify fire risk. Sensor data or a dataset with labeled fire occurrences is used for training. A simple GUI or web interface can show predictions in real time. The system helps in early detection and prevention of forest fires.
